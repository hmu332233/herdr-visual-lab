import type { GameEvent } from '../../../shared/events.js';import type { TimelineCursor } from '../../../shared/protocol.js';import type { GameFormat } from '../../format.js';import { createChrome } from './chrome.js';import { createStandingsPanel } from './standings.js';import { createFoundryScene } from './scene.js';import { foldFoundry,initialFoundry,projectFoundry,type FoundryState } from './fold.js';
export interface FoundryView{connection:FoundryState['connection'];timelineTime:number;timelineRate:number;teams:ReturnType<typeof projectFoundry>}
export function createFoundryStateOwner(){let state=initialFoundry(),cursor:TimelineCursor={timelineTime:0,timelineRate:1};return{onEvents(events:readonly GameEvent[],reset:boolean){if(reset)state=initialFoundry();for(const event of events)foldFoundry(state,event);},onTimeline(next:TimelineCursor){cursor=next;},view:():FoundryView=>({connection:state.connection,timelineTime:cursor.timelineTime,timelineRate:cursor.timelineRate,teams:projectFoundry(state,cursor.timelineTime)})};}
export function createFoundryFormat():GameFormat{const owner=createFoundryStateOwner();return{onEvents:owner.onEvents,onTimeline:owner.onTimeline,createChrome(){const c=createChrome();return{render:()=>c.render(owner.view())};},createStandings(el,onFocus){const c=createStandingsPanel(el,onFocus);return{render:()=>c.render(owner.view())};},createScene(canvas,onFocus){const c=createFoundryScene(canvas,onFocus);return{commit:()=>c.commit(owner.view()),frame:c.frame,resize:c.resize};}};}
