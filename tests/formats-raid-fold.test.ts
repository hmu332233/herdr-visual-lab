import{describe,it,expect}from'vitest';import{advanceRaidTo,replayRaid,setRaidCursor}from'../src/web/formats/raid/fold.js';import{projectRaid}from'../src/web/formats/raid/view.js';import{connectionChanged,eventHistory,snapshotApplied,teamJoined,unitJoined}from'./helpers/events.js';
const history=eventHistory([0,teamJoined()],[0,unitJoined()],[0,snapshotApplied()],[0,connectionChanged({kind:'live'})]);
describe('Raid fold',()=>{it('derives boss HP from damage',()=>{const s=replayRaid(history);setRaidCursor(s,{timelineTime:9,timelineRate:1});const v=projectRaid(s);expect(v.leaderDamage).toBeGreaterThan(0);expect(v.bossHpFraction).toBeCloseTo(1-v.leaderDamage/58,9);});it('is partition independent',()=>{const a=replayRaid(history),b=replayRaid(history);advanceRaidTo(a,2100);for(let t=.25;t<=2100;t+=.25)advanceRaidTo(b,t);expect(projectRaid(a)).toEqual(projectRaid(b));});});
