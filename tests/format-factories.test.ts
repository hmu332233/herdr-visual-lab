import{describe,it,expect}from'vitest';import{createF1StateOwner}from'../src/web/formats/f1/index.js';import{createRaidStateOwner}from'../src/web/formats/raid/index.js';import{createFoundryStateOwner}from'../src/web/formats/foundry/index.js';import{connectionChanged,eventHistory,snapshotApplied,teamJoined,unitJoined}from'./helpers/events.js';
const events=eventHistory([0,teamJoined()],[0,unitJoined()],[0,snapshotApplied()],[0,connectionChanged({kind:'live'})]);const cursor={timelineTime:10,timelineRate:1};
describe('format state owners',()=>{it('owns fresh deterministic state per factory call',()=>{for(const factory of[createF1StateOwner,createRaidStateOwner]){const a=factory(),b=factory();a.onEvents(events,true);b.onEvents(events,true);a.onTimeline(cursor);b.onTimeline(cursor);expect(a.view()).toEqual(b.view());}});it('projects Foundry from timeline time',()=>{const owner=createFoundryStateOwner();owner.onEvents(events,true);owner.onTimeline({timelineTime:5,timelineRate:1});expect(owner.view().teams[0].resources).toBe(10);owner.onTimeline(cursor);expect(owner.view().teams[0].resources).toBe(20);});});
