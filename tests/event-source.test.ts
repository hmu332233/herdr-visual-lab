import {describe,it,expect}from'vitest';import {createEventSource}from'../src/web/event-source.js';import type{GameEvent}from'../src/shared/events.js';
const ev=(seq:number):GameEvent=>({seq,at:seq,kind:'snapshot-applied'});const msg=(type:'history'|'sync',events:GameEvent[],time=0)=>({type,events,serverTime:0,timelineTime:time,timelineRate:1} as const);
describe('event source',()=>{it('validates gaps and forwards empty cursor deltas',()=>{const updates:number[]=[];const gaps:number[][]=[];const s=createEventSource((e,_r,c)=>updates.push(e.length+c.timelineTime),(a,b)=>gaps.push([a,b]));s.ingest(msg('history',[ev(1)],1));s.ingest(msg('sync',[],2));s.ingest(msg('sync',[ev(3)],3));expect(updates).toEqual([2,2]);expect(gaps).toEqual([[2,3]]);});});
