import{describe,it,expect}from'vitest';import{advanceF1To,initialF1State,replayF1,setF1Cursor}from'../src/web/formats/f1/fold.js';import{projectF1}from'../src/web/formats/f1/view.js';import{connectionChanged,eventHistory,snapshotApplied,teamJoined,unitJoined}from'./helpers/events.js';
const history=eventHistory([0,teamJoined()],[0,unitJoined()],[0,snapshotApplied()],[0,connectionChanged({kind:'live'})]);
describe('F1 fold',()=>{it('scores working cars from the neutral cursor',()=>{const s=replayF1(history);setF1Cursor(s,{timelineTime:9,timelineRate:2});const car=projectF1(s).teams[0].entries[0];expect(car.officialProgress).toBeGreaterThan(0);expect(car.displaySpeed).toBeGreaterThan(0);});it('is partition independent across lifecycle boundaries',()=>{const direct=replayF1(history),partitioned=replayF1(history);advanceF1To(direct,2100);for(let t=.25;t<=2100;t+=.25)advanceF1To(partitioned,t);expect(projectF1(direct)).toEqual(projectF1(partitioned));});});
